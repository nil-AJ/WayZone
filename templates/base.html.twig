<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <title>{% block title %}Welcome!{% endblock %}</title>
  <link rel="stylesheet" href="https://bootswatch.com/4/flatly/bootstrap.min.css">
  {% block stylesheets %}{% endblock %}
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{path('main')}}">Navbar</a>

    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="{{path('inscription')}}">Inscription sur le site <span class="sr-only">(current)</span></a>
        </li>
        {% if is_granted('IS_AUTHENTICATED_FULLY')==false %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('connexion')}}">Connexion</a>
        </li>
        {% endif %}

        {% if is_granted('ROLE_DEVELOPER')%}
        <li class="nav-item">
          <a class="nav-link" href="{{path('developers')}}">Developer Section</a>
        </li>
        {% endif %}

        {% if is_granted('ROLE_ADMIN')%}
        <li class="nav-item">
          <a class="nav-link" href="{{path('user_inscription')}}">Inscription Admin</a>
        </li>
        {% endif %}

        {% if is_granted('IS_AUTHENTICATED_FULLY')%}
        <li class="nav-item">
          <a class="nav-link" href="{{path('logout')}}">Deconnexion</a>
        </li>
        {% endif %}

      </ul>
      {% if is_granted('IS_AUTHENTICATED_FULLY')%}
      <span class="badge badge-dark">Bienvenue {{app.user.firstname}}</span>
      {% endif %}
    </div>
  </nav>
  {% block body %}{% endblock %}
  {% block javascripts %}{% endblock %}
  <footer>

  </footer>
</body>

</html>